# 0006 — 代码与文档清理

- 日期：2026-09-13
- 变更类型：`Removed` / `Changed` / `Docs`
- 范围：后端内部接口、前端样式与类型检查、文档导航

## 变更

- 删除未引用的 Runtime/Application Protocol、审计列表类型和内部查询方法。
- 删除未调用的独立审批状态/租约包装方法；实际恢复和提交仍使用同一事务内的 locked 方法。
- 删除空的 `management.css` 及其导入、未使用的变量/参数和相同分支的表达式。
- 移除无页面引用的旧按钮、配置、流程图和时间线样式，以及未使用/被覆盖的动画；保留现行选择器的声明和顺序。
- 开启前端未使用变量/参数检查；后端本地与 CI 的 Ruff 检查纳入 `scripts/`。
- 用 `backend/docs/testing.md` 统一自动化和真实模型验证步骤，删除已完成的生命周期实施计划与两环境验证计划。
- 精简前端文档为当前架构和管理交互，删除重复 Admin API 入口及未实施的接口/目录清单。
- `TODO.md` 只保留未完成工作；公开契约、兼容 Graph、参考插件和已有变更历史继续保留。

## 验证

- 后端 Ruff、格式检查、完整 fake-model 测试与制品构建。
- 前端未使用代码检查、类型检查与 production build。
- 仓库 Markdown 本地链接和 diff 空白检查。

删除内容仍可从 Git 历史恢复；本次不包含真实模型或生产数据验证。
